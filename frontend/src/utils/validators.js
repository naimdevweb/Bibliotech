/**
 * Fonctions de validation
 */

/**
 * Valide un email
 * @param {string} email
 * @returns {boolean}
 */
export function isValidEmail(email) {
  // TODO: Regex email
}

/**
 * Valide un mot de passe (min 8 caractères, 1 majuscule, 1 chiffre, 1 spécial)
 * @param {string} password
 * @returns {object} { isValid: boolean, errors: string[] }
 */
export function validatePassword(password) {
  // TODO: Vérifier toutes les règles
}

/**
 * Valide un ISBN (13 chiffres)
 * @param {string} isbn
 * @returns {boolean}
 */
export function isValidISBN(isbn) {
  // TODO: Vérifier format ISBN
}
