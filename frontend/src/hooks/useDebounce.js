/**
 * Hook useDebounce - Délai avant exécution (pour recherche en temps réel)
 *
 * Permet d'éviter trop d'appels API lors de la saisie
 *
 * Exemple d'utilisation :
 * const debouncedSearchTerm = useDebounce(searchTerm, 500);
 *
 * @param {*} value - Valeur à debouncer
 * @param {number} delay - Délai en ms (défaut 500)
 * @returns {*} Valeur debouncée
 */

export function useDebounce(value, delay = 500) {
  // TODO: Implémenter avec useState, useEffect, setTimeout
}
