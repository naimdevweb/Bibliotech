/**
 * Hook usePagination - Gestion de la pagination
 *
 * Fournit :
 * - currentPage : page actuelle
 * - totalPages : nombre total de pages
 * - goToPage(page) : aller à une page
 * - nextPage() : page suivante
 * - previousPage() : page précédente
 *
 * @param {number} totalItems - Nombre total d'éléments
 * @param {number} itemsPerPage - Éléments par page (défaut 20)
 */

export function usePagination(totalItems, itemsPerPage = 20) {
  // TODO: Implémenter avec useState
}
