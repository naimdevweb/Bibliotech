/**
 * Composant BookList - Liste de livres avec grille
 *
 * Affiche :
 * - Grille de BookCard
 * - Pagination
 * - Message si aucun livre trouvé
 *
 * Props :
 * - books: tableau de livres
 * - totalPages: nombre total de pages
 * - currentPage: page actuelle
 * - onPageChange: fonction appelée au changement de page
 */

export default function BookList({ books, totalPages, currentPage, onPageChange }) {
  // TODO: Code à implémenter
}
