/**
 * Composant ErrorMessage - Message d'erreur
 *
 * Affiche un message d'erreur stylisé (fond rouge)
 *
 * Props :
 * - message: texte de l'erreur
 * - onClose: fonction pour fermer (optionnel)
 */

export default function ErrorMessage({ message, onClose }) {
  // TODO: Code à implémenter
}
