/**
 * Composant Loading - Indicateur de chargement
 *
 * Affiche un spinner avec message optionnel
 *
 * Props :
 * - message: texte à afficher (optionnel)
 * - size: 'sm' | 'md' | 'lg'
 */

export default function Loading({ message, size = 'md' }) {
  // TODO: Code à implémenter (spinner CSS ou librairie)
}
