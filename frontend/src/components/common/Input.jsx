/**
 * Composant Input - Champ de saisie réutilisable
 *
 * Props :
 * - type: 'text' | 'email' | 'password' | 'number' | 'date'
 * - label: label du champ
 * - value: valeur
 * - onChange: fonction appelée au changement
 * - placeholder: placeholder
 * - error: message d'erreur
 * - required: booléen
 */

export default function Input({ type = 'text', label, value, onChange, placeholder, error, required }) {
  // TODO: Code à implémenter
}
