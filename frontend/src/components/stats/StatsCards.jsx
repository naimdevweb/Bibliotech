/**
 * Composant StatsCards - Cartes KPI (indicateurs clés)
 *
 * Affiche des cartes avec :
 * - Icône
 * - Valeur (nombre)
 * - Label
 * - Évolution (optionnel : +5% depuis le mois dernier)
 *
 * Props :
 * - stats: tableau { label, value, icon, evolution }
 */

export default function StatsCards({ stats }) {
  // TODO: Code à implémenter
}
